# This workflow has been replaced by build-macos.yml, build-linux.yml, and build-windows.yml.
# This file can be deleted once you've confirmed the new workflows are working.
name: Build (deprecated)
on:
  workflow_dispatch:
jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is deprecated. Use build-macos.yml, build-linux.yml, or build-windows.yml."
